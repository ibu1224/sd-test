annotations:
  screwdriver.cd/chainPR: true

shared:
  image: node:10
 
jobs:
  build1:
    requires: [ ~commit, ~pr ]
    steps:
      - hello: echo "$PR_BRANCH_NAME"
  build2:
    requires: [ build1 ]
    steps:
      - hello: echo "Hello build2"
